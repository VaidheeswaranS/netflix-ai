import React from "react";

const MovieDetailsBackground = ({ movieId }) => {
  return <div className="text-white">MovieDetailsBackground for {movieId}</div>;
};

export default MovieDetailsBackground;
